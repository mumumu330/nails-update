<div class="whiteback">
    <div class="whiteblock">
        <h3 class="text-center">通知</h3>
            <%# <%= link_to destroy_all_notifications_path, method: :delete do %> 
                <%# <i class="fas fa-trash" style="color: black;"></i> %>
                <%# <h7 style="color: black;">全削除</h7> %>
            <%# <% end %> 
            <hr>
            <% if @notifications.exists? %>
                <div class="users-index">
                    <% @notifications.each do |notification|  %>
                        <%#= render 'notifications/notification', notification: @notifications %>
                        <%= attachment_image_tag notification.visiter, :profile_image, :fill,100,100, format: "jpeg", fallback: "no_image.jpg", size: "50x50" %>
                        <%= notification.display_text %>
                        <%= notification.created_at.strftime('%Y/%m/%d') %>
                        <br>
                    <% end %>
                </div>
            <% else %>
                <p>通知はありません</p>
            <% end %>
    </div>
</div>